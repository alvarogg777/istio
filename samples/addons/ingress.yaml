apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: istio-system
  namespace: istio-system
  annotations:
    kubernetes.io/ingress.class: istio
spec:
  rules:
  - host: grafana.alchys.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana
            port:
              number: 3000
  - host: tracing.alchys.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tracing
            port:
              number: 16686
  - host: prometheus.alchys.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: prometheus
            port:
              number: 9090
  # - host: auth.alchys.tech
  #   http:
  #     paths:
  #     - path: /
  #       pathType: Prefix
  #       backend:
  #         serviceName: authelia-service
  #         servicePort: 80

